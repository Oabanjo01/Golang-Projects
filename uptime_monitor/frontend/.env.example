# Copy to .env.local to change these for your machine.

# Run the UI against the in-memory mock in src/mocks/ instead of the Go API.
# Set to false (or remove) once the backend is running on :8080.
VITE_API_MOCK=true
