# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Compiled example/CLI binaries (built with `go build`, no extension on macOS/Linux)
task-tracker/task-cli
examples/helloworld/Helloworld
examples/threadingAndConcurrency/threadingAndConcurrency

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env file
.env

# runtime data
task-tracker/tasks.json

# macOS
.DS_Store
